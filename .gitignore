# Python / venv
.venv/
.venv_tflite/
__pycache__/
*.pyc
*.pyo

# Datasets (shared, read-only; never committed)
datasets/
dut_yolo/
*.zip

# Training outputs
runs/
*.cache

# Logs / scratch
.venv_install.log
.torch_reinstall.log
*.log

# Pretrained checkpoints auto-downloaded to repo root (not our artifacts)
/yolo26n.pt
/yolo26s.pt
/yolo26*.pt

# C++ ncnn build dirs (host/cross). ncnn source tree & Android NDK live OUTSIDE the repo.
cpp/build/
cpp/build-ml2/

# OS / editor / tooling
.DS_Store
.claude/

# NOTE: weights/ is intentionally tracked (best.pt, *.onnx, *.tflite, metrics).
# Large single artifacts (>50MB) go to Git LFS or a GitHub Release (see README).
